---
# tasks file for gdm
- name: install dependencies
  kewlfft.aur.aur:
    name: "{{ gdm_packages }}"
    use: paru

- name: enable GDM service
  become: true
  systemd:
    name: gdm.service
    enabled: yes
