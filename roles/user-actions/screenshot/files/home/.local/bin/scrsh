#!/usr/bin/bash

# 1. PrintScreen btn
# 2. Select mode (screen, window, area) with eww or rofi or etc...
# 3. make screenshot and pass to clipboard
# 4. Select action (save to file, edit, upload) with eww or rofi or etc...

# save selected window to clipboard
maim -s | xclip -selection clipboard -t image/png

# save file from clipboard
xclip -selection clipboard -t image/png -o > $(xdg-user-dir PICTURES)/$(date +%s).png

# notify
notify-send 'scrsh' 'Copied to clipboard' -i $(xdg-user-dir PICTURES)/$(date +%s).png

# open clipboard image in Annotator
com.github.phase1geo.annotator --use-clipboard
