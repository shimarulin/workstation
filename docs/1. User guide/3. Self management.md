# Self configuration management

Get latest changes:

```shell
cd ~/.config/workstation
git pull
```

Make sure that all requirement installed and upgraded:

```shell
./tools/bin/install --no-setup
```
